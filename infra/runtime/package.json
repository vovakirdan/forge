{
  "name": "forge-managed-cli-runtimes",
  "private": true,
  "version": "0.1.0",
  "description": "Pinned image-only CLI dependencies; never installed into the host agent home",
  "dependencies": {
    "@openai/codex": "0.153.2",
    "opencode-ai": "1.18.29"
  }
}
