# M3 execution ledger

**Статус:** implemented and accepted; M3 closed on 11 September 2026 within the approved scope
**Contract:** [M3 specs](2026-09-11-m3-specs.md)

| ID | Epic / deliverable | Status |
| --- | --- | --- |
| M3-01 | Canonical knowledge/memory/revisions/commands | implemented; 6 PG tests passed |
| M3-02 | AgentMemory upsert/strict import/isolated build | implemented; real isolated index acceptance passed |
| M3-03 | Forge AgentMemory HTTP adapter | implemented; 11 synthetic HTTP tests passed |
| M3-04 | Durable projection/revalidation/rebuild | implemented; 4 PG + HTTP tests passed |
| M3-05 | SystemJob RunSpec/ownership/admission/protocol | implemented; 4 PG ownership tests and protocol/domain tests passed |
| M3-06 | SystemJob dispatch/result/stop/recovery/quotas | implemented; 3 lifecycle, 2 full Gateway and 2 Core restart tests passed |
| M3-07 | Summarizer/coalescing/sources/backfill | implemented; full Gateway and producer regression passed |
| M3-08 | Knowledge CLI/API/context compiler | implemented; 4 PG/UDS context tests passed |
| M3-09 | Onboarding gate/note/receipt/retry/skip/legacy | implemented; real PG/UDS onboarding and 3 admission regressions passed |
| M3-10 | Keyless integration and bounded Codex evidence | passed; 29 keyless tests, real index and 3 approved gpt-5.6-luna Runs |
| M3-11 | Independent reviews/fixes/docs/regression | passed; findings fixed and final serial gates green |

## Boundaries

Forge implementation base: main at 5b85f23. AgentMemory dedicated worktree
/home/zov/projects/agentmemory-forge-m3, branch forge/m3-indexing, base 734c178.
Dependency commit: `c65e93e31e8865ac4843360e4560c077a93d010b`, published on
11 September in `https://github.com/vovakirdan/agentmemory.git`,
branch `forge/m3-indexing`. The dependency's `main` branch was not changed.
Built image: `sha256:c5de2bf9252fa8ef251b7025e44a3d9a13d6c61bacbe3c0dcc9b65900552da18`.
Original personal checkout/daemon/data are untouched.

## Evidence

11 September, implementation, regression and bounded live acceptance evidence:

- Canonical knowledge: 6 real PostgreSQL/NATS tests passed in private schemas.
- AgentMemory HTTP adapter: 11 synthetic HTTP tests passed, including partial
  acknowledgement, old-server rejection, redirect containment and body deadline.
- Projection: 4 real PostgreSQL + synthetic HTTP tests passed: retry/rebuild,
  late acknowledgement/withdrawal, project/Employee scopes, canonical fallback
  and owner-only HTTP API.
- Isolated AgentMemory source build: local MiniLM inference returned 384 finite
  dimensions. Dedicated offline runtime proved strict import/replay, update,
  project filtering, restart persistence and deletion. Fork test suite:
  1757 passed, 1 skipped; build passed; TypeScript diagnostics match the baseline.
- Real Forge-to-AgentMemory acceptance, without provider credentials or Runs:
  `/tmp/fm3.1eC0IIfq/evidence/report.json`, `outcome=passed`,
  `cleanup_failed=false`. Restart/readback, outage/fallback and backlog catch-up
  passed; final pending count is zero. Retained index and session are stopped.
  Repeated on final code: `/tmp/fm3.36GwQTAI/evidence/report.json`, also passed;
  zero provider Runs, the isolated index is physically stopped.
- Context: 4 PostgreSQL/NATS/UDS tests proved the same frozen knowledge in actual
  prepared provider stdin and Task/Communication/Resolution manifests, canonical
  authority during index outage, explicit overflow refusal and immutable refresh.
- SystemJobs: 4 ownership tests, 3 lifecycle tests and 2 full Gateway tests passed.
  The Gateway tests use actual source compilation and prepared V7 input, but
  synthetic provider output and Supervisor observations, not live Codex inference.
- Independent fork review found non-finite embeddings incorrectly counted as
  indexed; finite-value guards and regression tests were added.
- SystemJob ownership review found approved-binding and cross-project onboarding
  FK gaps; both were fixed before integration verification. Review also found
  pending Employee admission gaps outside Task dispatch, skipped onboarding retry
  and producer-specific Run-to-Task event mapping; fixes have dedicated regressions.
- Historical M0 CLI demo and M1/M2 launchers now explicitly skip onboarding with an audited reason;
  the M3 launcher does not use this bypass. Existing persisted Employees retain
  `legacy_bypass`, never a fabricated completion receipt.
- Broad regression first passed 14 real Podman tests and 57 PostgreSQL command
  conformance tests. It caught an outdated MCP catalog assertion (11 tools before
  the three memory tools); the test now checks 14 tools, their exact memory names
  and absence of SystemJob result submission in a Task Run.
- Full regression exposed a real M1 stack overflow in file snapshot processing.
  Gdb showed nested async scheduler state, not recursion. Heap-pinning the inner
  scheduler at its existing awaited boundary reduced `dispatch_available` from
  27,680 to 208 bytes and `file_snapshot_tick` from 31,376 to 7,280 bytes
  (debug DWARF, Rust 1.98.0). There is no task spawn or enlarged thread stack.
  A bounded-future unit regression was added; all 7 M1 runtime tests then passed,
  including the exact physical snapshot regression. Evidence:
  `/tmp/forge-m3-stack.7UTkT6d6/diagnosis.md` and
  `/tmp/forge-m3-validation.sGjWHwLB/fixes-final.log`.
- The existing QA-writer fixture also created an Employee directly; it now makes
  its historical onboarding skip explicit rather than weakening production gates.
  All 26 keyless Git proposal/integration tests then passed (live gate filtered out).
- Both replacement-Core tests passed with real UDS Gateway acceptance and the
  same sealed/pinned synthetic credential. Old V7 authority is not restored;
  retry waits for physical quiescence; a previously accepted result/receipt
  survives and materializes once. The fixture verifies canonical force-stop and
  exact Run/fence/epoch while allowing at-least-once graceful-stop delivery.
- Actual M0 CLI demo passed after the compatibility update (`done`, one Run,
  one Artifact): `/tmp/forge-m0-runtime.kCzrlh`. Both real MinIO evidence/snapshot
  tests passed, including empty/binary round-trip and immutable replay.
- Full `cargo test --workspace --all-features --locked --no-fail-fast`
  passed: 583 tests, including doctests; service-backed ignored tests run separately.
  The earlier concurrent-build doctest artifact race was not reproduced in the
  serialized run. Full log: `/tmp/forge-m3-validation.sGjWHwLB/workspace.log`.
- Final serialized service-backed regression passed: 213 tests across Core,
  Storage, Supervisor and Testkit, including all 29 M3 tests in nine suites.
  Paid/live provider gates were explicitly filtered out; the two MinIO tests
  were verified separately as described above. Full log:
  `/tmp/forge-m3-validation.sGjWHwLB/integration-final.log`.
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`,
  `cargo fmt --all -- --check` and `bash scripts/tests/m3-launcher-test.sh` passed.
  Their logs are `clippy.log`, `fmt.log` and `shell.log` in the same directory.
- Final independent read-only checks found no remaining blockers. The launcher
  and acceptance guide now say that the attempt limit is per job generation,
  with a separate rolling 24-hour cap; new sources can create a new generation.
  Generated graph data stays ignored; no credentials are included in the changes.

## Bounded Codex acceptance

The user approved `gpt-5.6-luna` with the existing `/home/zov/.codex/auth.json`.
Command:

```bash
bash scripts/run-m3.sh run --existing-services \
  --model gpt-5.6-luna --auth-file /home/zov/.codex/auth.json --yes
```

Evidence: `/tmp/fm3.SpnLckre/evidence/report.json`, `outcome=passed`, exit 0,
`cleanup_failed=false`. Exactly three Runs used that model: ownerless onboarding,
ordinary Task and ownerless summarization. All have desired/observed `stopped`;
the dedicated index container is also physically stopped. Session data is retained.

- Onboarding produced a personal note with three source references and a receipt.
  Only then was the new Employee admitted to its Task.
- Task reached `done` with one stage Artifact, no WaitCondition and an actual
  executable `normalize-lines.sh` in its isolated worktree. After reading the
  three-line script, the operator checks also passed syntax, duplicate/sort and
  empty-input cases; this is not a universal Forge project-testing feature.
- Summarizer produced a TaskSummary with eight source references and Event
  coverage 23–41, explicitly attributing reported observations rather than
  asserting that it independently verified them.
- Final index status: indexed 4, pending 0, retired 2. Two failed projection
  attempts were caused by the deliberate outage; catch-up completed.
- Named project stop retained a newly requested summary generation as pending;
  the six-second observation window saw no new Run (3 before, 3 after).
- Independent review compared the actual provider stdin with the frozen manifests:
  two published Policy revisions and the Employee's personal note matched exactly;
  all three instructions and both SystemJob inputs matched their pins. SystemJobs
  had only `read` and `submit_result` Gateway grants.

The real Employee's three direct `memory.read` attempts used UUIDs with one
incorrect group generated by the model. Core correctly rejected them; the correct
IDs were present in stdin, and real indexed `memory.search` succeeded. This is not
proof of a successful live `memory.read`; successful reads and isolation are
covered by the keyless Gateway tests. Review found no functional read defect.
The generic error text incorrectly mentioned Task for any missing resource: it
was made resource-neutral after the live run, with a non-disclosure regression.
All 77 current Core library tests, strict workspace Clippy and formatting then
passed. Logs: `core-lib-final.log`, `gateway-diagnostic.log`, `clippy-final.log`
under `/tmp/forge-m3-validation.sGjWHwLB`.

No M2 provider live gate, other model/profile, installer or UI is claimed by this
acceptance. The user requested publication of Forge and the dependency after
acceptance. Pre-publication checks passed again: all 77 Core library tests,
strict workspace Clippy, formatting, M3 launcher contracts and 56 focused
AgentMemory indexing tests. Independent commit hygiene found no secrets or
generated runtime data in the changes. Private acceptance sessions stay local.
